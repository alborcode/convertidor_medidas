// CLASE ESTATICA PARA ESTILOS

import 'package:flutter/material.dart';

class Estilos{

  // Definicion estatica de Estilo
    static final estiloLabel = TextStyle(
        color: Colors.blueGrey,
        fontSize: 20
    );
    static final estiloMedida = TextStyle(
        color: Colors.deepOrange[700],
        fontSize: 20
    );

}